export enum PageEnum {
  // basic login path
  BASE_LOGIN = '/login',
  // basic home path
  // BASE_HOME = '/dashboard/welcome',
  // BASE_HOME = '/manager/department',
  BASE_HOME = '/home',
  // error page path
  ERROR_PAGE = '/exception',
}
